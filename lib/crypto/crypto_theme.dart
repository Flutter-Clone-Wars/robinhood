import 'dart:ui';

/// The background color of a Robinhood crypto coin screen.
const screenBackground = Color(0xFF120821);

/// The accent color used throughout the crypto UI when the
/// coin price goes up.
const priceIncreaseColor = Color(0xFFc1f755);

/// The accent color used throughout the crypto UI when the
/// coin price goes down.
const priceDecreaseColor = Color(0xFFff5a89);
